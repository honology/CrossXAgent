# Pinned (not "stable") because sysinfo 0.39 requires rustc >= 1.95 while the
# machine-wide stable toolchain may lag; a pin keeps builds reproducible.
[toolchain]
channel = "1.97.0"
components = ["clippy", "rustfmt"]
